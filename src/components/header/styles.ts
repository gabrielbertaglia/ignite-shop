import { styled } from '@stitches/react'

export const HeaderContainer = styled('header', {
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  padding: '2rem 0',
  width: '100%',
  maxWidth: 1180,
  margin: '0 auto',

  button: {
    marginLeft: 'auto',
  },
})
